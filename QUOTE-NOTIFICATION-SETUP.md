# 見積もり依頼メール通知の設定

ハーネス見積もりフォームは、依頼内容をデータベースへ保存した後、担当者へのメール通知を試みる構成です。現在は担当者メールアドレスが未決定のため、**通知先は空欄**になっています。空欄の間も依頼自体は保存され、管理画面 `/admin/quotes` で確認できます。

## 通知先を決めた後に変更する場所

`server/quoteNotification.ts` の次の2項目を編集します。

```ts
recipientEmail: "", // 担当者の受信メールアドレス
senderEmail: "",    // 送信元として認証済みのメールアドレス
```

## メール送信を有効にする手順

1. 送信元ドメインをメール送信サービスで認証する。
2. `recipientEmail` と `senderEmail` を設定する。
3. `RESEND_API_KEY` をプロジェクトのシークレットとして登録する。
4. テスト依頼を送信し、管理画面の依頼と担当者メールの両方を確認する。

> 通知先またはメール送信サービスの設定が未完了の場合、フォームは送信失敗にせず、依頼を保存して通知ステータスを `skipped` と記録します。

## 変更可能な見積もり項目

質問文、選択肢、添付上限、返信案内は `client/src/content/harnessQuote.ts` で変更できます。画面本体やサーバー処理を変更する必要はありません。
