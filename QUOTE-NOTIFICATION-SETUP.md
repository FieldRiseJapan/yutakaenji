# 見積もり依頼メール通知の設定

ハーネス見積もりフォームは、依頼内容を保存した後に**2通のメール**を送る構成です。1通目は担当者向けの即時通知、2通目は依頼者向けの受付完了メールです。

現在は担当者メールアドレスと送信元アドレスが未設定のため、依頼は安全に保存され、通知状態は `skipped` と記録されます。通知先を決めるまでは依頼を取りこぼさず、管理画面 `/admin/quotes` から確認できます。

## 通知先を決めた後に変更する場所

`server/quoteNotification.ts` の次の3項目を編集します。

```ts
recipientEmail: "", // 新規依頼を受信する担当者のメールアドレス
senderEmail: "",    // メール送信サービスで認証済みの送信元アドレス
replyToEmail: "",   // 依頼者からの返信を受けるアドレス（通常は担当者と同じ）
```

## メール送信を有効にする手順

1. メール送信サービスで送信元ドメインを認証します。
2. `recipientEmail`、`senderEmail`、`replyToEmail` を設定します。
3. `RESEND_API_KEY` をプロジェクトのシークレットとして登録します。
4. テスト依頼を送信し、担当者通知と依頼者向け受付完了メールの両方を確認します。

> 2通のうち1通でも送信に失敗した場合、依頼自体は保存し、通知状態を `failed` と記録します。担当者は管理画面から依頼内容を確認できます。

## 依頼者へ送る受付完了メールの内容

依頼者には、受付番号、相談内容、希望数量、希望納期、担当者からの連絡目安を送信します。価格を自動で提示せず、内容を確認した後に個別対応する運用です。
